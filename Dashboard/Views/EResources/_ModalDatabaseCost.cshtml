@model Dashboard.ViewModels.EResDatabaseCostViewModel
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "formDbCost", @class = "form-horizontal" }))
{    
    <div class="modal-header">
		<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
		<h3>Database Year Cost Details</h3>
	</div>
    <div class="modal-body">
		@Html.AntiForgeryToken() 
		@Html.ValidationSummary(true)

		@Html.Hidden("inputMode")
        @Html.HiddenFor(m => m.DatabaseId)

        @Html.FormLineDropDownListFor(m => m.Year, (IEnumerable<SelectListItem>) @ViewBag.Years)
        @Html.FormLineTextBoxFor(m => m.Cost)
    </div>
	<div class="modal-footer">
		<a href="#" id="CancelButton" class="btn" aria-hidden="true">Close</a>
		<a href="#" id="SaveButton" class="btn btn-primary">Save</a>
	</div>
}

<script type="text/javascript">
    $(function () {
        /* =======================================================================
         * Button handlers
         */
        $('#SaveButton').click(function () {

        });

        $('#CancelButton').click(function() {

        });

        /* =======================================================================
         * Methods called by handlers
         */
        function saveCost() {
            // Save the new cost information to the database
        }

        function updateCost() {
            // Update the existing cost information to the database
        }

        function deleteCost() {
            // Delete (soft delete) cost information in the database
        }
    })
</script>